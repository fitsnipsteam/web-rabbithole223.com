version: 1
frontend:
  phases:
    build:
      commands: 
        - "echo HUGO_ENV: $HUGO_ENV"
        - "hugo --environment $HUGO_ENV"
  artifacts:
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths: []
